请你参考 crates\backend\nomifun-requirement  和 crates\backend\nomifun-knowledge                                                                                这两个模块。帮我再wiki里面写一个开发流程的说明文档这两个是后台模块，前端模块再ui\src 里面。需要你写一个前后端的开发流程文档。这两个是业务类型的所以我挑选来做 
  代表，你的文档需要有普适性。然后需要你文档里面输出的内容是。后端部分，功能拓扑图，文件目录命名规范，数据库规范（目录，文件等），api接口规范 里面。需要你写一  
  个前后端的开发流程文档。这两个是业务类型的所以我挑选来做代表，你的文档需要有普适性。然后需要你文档里面输出的内容是。后端部分，功能拓扑图，文件目录命名规范，  
  数据库规范（目录，文件等）。前端的有如何暴露api（或者说注册），page页面命名规范，组件规范，样式规范（需要统一样式）等，做一个开发wiki说明文档到知识库里面 



/brainstorming 我要做一个ai生成视频的功能，然后目前有doubao-seedance-2-0模型和kling-v3。我不要用数据库。这两个模型的调用参数不一样，但是我想统一接口。调用的api    key前端传递就行，后面我可以给你调用方法和参数说明。然后你看看还需要头脑风暴什么东西？    


❯ /brainstorming 我需要你帮我头脑风暴一下。我现在需要做一个无限画布的功能。这个功能主要是用来做生成图片生成文字生成视频的。他需要一个画布，然后我选择了flowgram这个 
库，需要你帮我加入到前端哪里，文档我给你准备好了在wiki\flowgram-demo-and-doc                                                                                        
里面。画布先支持文本，视频，图片，三种节点。然后画布右侧有一个工具栏，工具栏有这三种节点，点击就可以添加节点到画布。然后来说下画布功能。文本节点画布主要就是用来调  
用大模型，生成图片提示词这个作用，后面我给你大模型调用api。然后图片节点就是保留一张图片。然后这个图片节点点击的时候可以调用大模型，比如豆包seedream                 
2，这个大模型，系统已经有这块代码生图的。然后图片如果被链接，那么他支持获取链接他的图片节点的图片，然后他自己也可以加图片，也可以写提示词，提示词里面可以引用这些图 
片调用生图模型。视频节点可以先不做。就是图片节点的数据主要就是显示他自己，如果他上传了一张图片，那么就多一个图片节点链接他，保证每一个图片节点就只有一张图片，但是  
他可以拿到链接他的图片节点的图片，他里面可以大模型生图调用这些图片做参考，也可以设置生图参数，比如尺寸，然后如果生图的时候感觉图片不够，他可以添加图片，添加就类似  
加一个图片节点关联他。                                

● 好的，让我先创建任务清单，然后深入了解项目上下文和 Flowgram 文档。
     
     
● 正在深入探索项目结构和 Flowgram 文档，请稍等...

● 项目上下文和 Flowgram 文档都已了解完毕。我来总结一下我理解到的内容，然后开始提问。

  项目现状：
  - Tauri 2.x 桌面应用 + React 19 + Arco Design + Vite 6
  - 已有完整的图片生成流水线（Doubao Seedream 4.5 适配器，后端 Rust，前端 imageGeneration 页面）
  - UI 中已有 @xyflow/react 依赖，但选择了 Flowgram 作为画布方案
  - 侧边栏已有 Image Generation / Video Generation 入口
   
  你的需求理解：  1. 基于 Flowgram Free Layout 做无限画布
  2. 三种节点：文本、图片、视频（视频先不做）  3. 右侧工具栏，点击可添加节点到画布
  4. 文本节点：调用大模型生成提示词
  5. 图片节点：持有一张图片；可调用 Seedream 生图；可以接收上游图片节点的图片作为参考；可设置生图参数；上传图片时自动创建新的图片节点链接过来
  6. 核心规则：每个图片节点只有一张图片，但可以获取链接它的节点的图片做参考

  ---
  现在第一个问题：

● User answered Claude's questions:
  ⎿  · 无限画布功能是作为独立页面（类似现有的 imageGeneration 页面），还是嵌入到现有某个页面中？ → 新建独立页面 (Recommended)
     
● 好的，新建独立页面。下一个问题：

● User answered Claude's questions:
  ⎿  · 图片节点的编辑交互方式是怎样的？点击图片节点后，用户在哪里编辑提示词、设置生图参数、查看参考图片？ → 节点内展开编辑
     
● 明白，节点内展开编辑。下一个问题：

● User answered Claude's questions:
  ⎿  · 图片节点的图片来源方式是什么？用户如何给图片节点添加图片？ → 本地上传base64这种，点击上传那种
     
● 好的，本地上传 base64。下一个问题：

● User answered Claude's questions:
  ⎿  · 图片节点之间的连接方向和数据流向是怎样的？比如 A 连到 B，意思是 A 的图片作为 B 的参考图，还是双向关联？ → 可以双向，这个不限制用户就行。这个数据这块你可能需
     要看下文档可能和变量有关，就是节点可以拿上游的数据这种，如果双向那么就是进入自己图片节点的编辑的时候，可以拿到关联的图片咯，方便让生图模型修改，做微一个参考图
     。对了编辑区域不要在图片节点内部好了，看看又没办法悬浮一个编辑区域点击图片的时候显示出来，类似弹窗，这个渲染图片节点的时候，直接把图片渲染出来，节点整洁一点
    
● 好的，我理解了几个关键点：

  1. 双向连接，不限制用户方向，连接的图片节点都可作为参考图
  2. Flowgram 变量引擎可以用于节点间数据传递
  3. 编辑区域改为悬浮弹窗：图片节点只显示图片本身，点击时弹出悬浮编辑面板（提示词、参数、参考图列表等）
     
  下一个问题：

● User answered Claude's questions:
  ⎿  · 画布中的图片生图调用走哪条路径？现有的后端 Rust 接口还是前端直接调用 API？ → 复用后端 API (Recommended)
     
● 好，复用后端 API。再问一个关键问题：

● User answered Claude's questions:
  ⎿  · 当图片节点调用生图模型后，生成的图片怎么处理？是替换当前节点的图片，还是创建新的图片节点？ → 
     如果图片节点图片是空的就把图片设置上，如果原来有老图的，就生成新图片节点，然后关联一下，老->新
     
● 很好！逻辑很清晰：
  - 空图片节点 → 生图结果直接设为该节点图片
  - 有老图 → 创建新图片节点，老→新 连线

  最后一个问题：

● User answered Claude's questions:
  ⎿  · 画布数据需要持久化吗？用户关闭后重新打开应该能看到之前的画布吗？ → 前端先存储，他支持存储节点数据和连线数据的，你看下代码或者api，可以保存成json。所以你可以
     多画布，添加画布的时候起一个名字，然后就进入。保存的时候，保存json数据就可以。前端控制就行